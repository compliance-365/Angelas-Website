---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE } from '../../config/site';

const base = import.meta.env.BASE_URL;

const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: 'Reconciliation Action Plans (RAP)',
  provider: { '@type': 'Organization', name: '2Ways Consultancy' },
  areaServed: 'AU',
  description:
    'End-to-end RAP planning, delivery, evidence and reporting across Reflect, Innovate, Stretch and Elevate, led by Indigenous consultants.'
};
---
<BaseLayout
  title={`Reconciliation Action Plans (RAP) | ${SITE.name}`}
  description="Indigenous-led RAP strategy and delivery across Reflect, Innovate, Stretch, and Elevate — with governance, evidence and reporting baked in."
  canonical={`${SITE.url}/services/rap/`}
  schema={serviceSchema}
>
  <Fragment slot="header"><Header /></Fragment>

  <!-- HERO: parallax + glass card -->
  <section id="top" class="relative overflow-hidden">
    <div class="absolute inset-0">
      <div
        class="h-[48rem] bg-cover bg-center md:bg-fixed will-change-transform"
        style={`background-image:url('${base}hero-home.webp')`}
        data-parallax
        data-speed="0.15"
      ></div>
      <div class="absolute inset-0 bg-gradient-to-b from-black/65 via-black/40 to-black/30"></div>
    </div>

    <div class="relative mx-auto max-w-6xl px-4 h-[48rem] flex items-end pb-14">
      <div class="max-w-3xl text-white bg-white/10 backdrop-blur-md rounded-2xl p-6 border border-white/20">
        <p class="kicker">Indigenous Australian–led</p>
        <h1 class="text-4xl md:text-5xl font-extrabold leading-tight">Reconciliation Action Plans</h1>
        <p class="mt-3 text-white/90">
          We move RAPs from documents to outcomes — aligning Reflect, Innovate, Stretch and Elevate with clear governance,
          owners, timelines and measurable impact.
        </p>
        <div class="mt-6 flex flex-wrap gap-3">
          <a href={`${base}contact/`} class="btn">Book a RAP Discovery Call</a>
          <a href={`${base}services/dashboard/`} class="btn-outline">See the RAP Dashboard</a>
        </div>
      </div>
    </div>
  </section>

  <!-- STICKY SUB-NAV -->
  <nav class="subnav sticky top-[64px] z-40 border-b border-gray-200">
    <div class="mx-auto max-w-6xl px-4 py-2 flex flex-wrap gap-1">
      <a href="#overview">Overview</a>
      <a href="#outcomes">Outcomes</a>
      <a href="#stages">Stages</a>
      <a href="#process">Process</a>
      <a href="#deliverables">Deliverables</a>
      <a href="#timeline">Timeline</a>
      <a href="#faq">FAQ</a>
      <a href={`${base}contact/`}>Contact</a>
    </div>
  </nav>

  <!-- OVERVIEW -->
  <section id="overview" class="section">
    <h2 class="text-2xl font-semibold reveal">Why 2Ways for your RAP?</h2>
    <p class="mt-3 max-w-3xl text-gray-700 reveal">
      RAPs succeed when they’re co-designed, practical and measured. Our team blends Indigenous leadership and delivery
      experience across utilities, government and construction. We set up governance, define owners, and capture evidence
      in the <a href={`${base}services/dashboard/`} class="underline">RAP Dashboard</a> so your progress is always visible.
    </p>
  </section>

  <!-- OUTCOMES -->
  <section id="outcomes" class="section pt-0">
    <h2 class="text-2xl font-semibold reveal">What you’ll achieve</h2>

    <div class="grid md:grid-cols-3 gap-6 mt-6">
      <!-- Clear roadmap -->
      <div class="grid-card card-raise opacity-0 translate-y-4 reveal">
        <div class="flex items-center justify-between">
          <span class="pill-strong">Clear roadmap</span>
          <span class="icon-circle" aria-hidden="true">
            <!-- roadmap icon -->
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M3 5h13l5 4v10H3V5z" stroke="white" stroke-width="1.8"/><path d="M16 5v6h5" stroke="white" stroke-width="1.8"/></svg>
          </span>
        </div>
        <p class="text-sm mt-3">Owners, timelines, milestones and RAP governance cadence aligned to your operating model.</p>
        <ul class="list-disc ml-5 text-sm mt-3 text-gray-700">
          <li>Accountable owners & due dates</li>
          <li>Quarterly governance templates</li>
          <li>Change & comms support</li>
        </ul>
      </div>

      <!-- Evidence & reporting -->
      <div class="grid-card card-raise opacity-0 translate-y-4 reveal">
        <div class="flex items-center justify-between">
          <span class="pill">Evidence & reporting</span>
          <span class="icon-circle" aria-hidden="true">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M4 19h16M7 16V5m5 11V8m5 8V3" stroke="white" stroke-width="1.8"/></svg>
          </span>
        </div>
        <p class="text-sm mt-3">Proof points captured in SharePoint with quarterly snapshots ready for board reporting.</p>
        <ul class="list-disc ml-5 text-sm mt-3 text-gray-700">
          <li>Audit-friendly evidence</li>
          <li>Progress snapshots</li>
          <li>Export to Excel/Power BI</li>
        </ul>
      </div>

      <!-- Cultural uplift -->
      <div class="grid-card card-raise opacity-0 translate-y-4 reveal">
        <div class="flex items-center justify-between">
          <span class="pill">Cultural uplift</span>
          <span class="icon-circle" aria-hidden="true">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M12 3v18M3 12h18" stroke="white" stroke-width="1.8"/></svg>
          </span>
        </div>
        <p class="text-sm mt-3">Capability grows via targeted training & engagement, tracked against RAP goals.</p>
        <ul class="list-disc ml-5 text-sm mt-3 text-gray-700">
          <li>Mapped training pathways</li>
          <li>Engagement & supplier diversity</li>
          <li>KPIs tied to actions</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- STAGES -->
  <section id="stages" class="section">
    <h2 class="text-2xl font-semibold reveal">RAP stages we support</h2>

    <div class="grid md:grid-cols-4 gap-6 mt-6">
      <!-- Reflect -->
      <article class="stage-card p-5 opacity-0 translate-y-4 reveal card-raise">
        <div class="flex items-center justify-between">
          <h3 class="font-semibold">Reflect</h3>
          <span class="pill">Foundations</span>
        </div>
        <p class="text-sm mt-3">Baseline, governance & respectful protocols shaped with your stakeholders.</p>
        <ul class="text-sm mt-3 list-disc pl-5 text-gray-700">
          <li>Stakeholder mapping</li><li>Baseline & gap analysis</li><li>Governance setup</li>
        </ul>
      </article>

      <!-- Innovate -->
      <article class="stage-card p-5 opacity-0 translate-y-4 reveal card-raise">
        <div class="flex items-center justify-between">
          <h3 class="font-semibold">Innovate</h3>
          <span class="pill">Scale</span>
        </div>
        <p class="text-sm mt-3">Training, engagement & supplier diversity with clear measures and owners.</p>
        <ul class="text-sm mt-3 list-disc pl-5 text-gray-700">
          <li>Training roll-out</li><li>Engagement plans</li><li>Procurement goals</li>
        </ul>
      </article>

      <!-- Stretch -->
      <article class="stage-card p-5 opacity-0 translate-y-4 reveal card-raise">
        <div class="flex items-center justify-between">
          <h3 class="font-semibold">Stretch</h3>
          <span class="pill">Embed</span>
        </div>
        <p class="text-sm mt-3">Practice embedded with measurable improvement against KPIs.</p>
        <ul class="text-sm mt-3 list-disc pl-5 text-gray-700">
          <li>KPI tracking</li><li>Leadership accountability</li><li>Quarterly reviews</li>
        </ul>
      </article>

      <!-- Elevate -->
      <article class="stage-card p-5 opacity-0 translate-y-4 reveal card-raise">
        <div class="flex items-center justify-between">
          <h3 class="font-semibold">Elevate</h3>
          <span class="pill">Lead</span>
        </div>
        <p class="text-sm mt-3">Sector leadership & public accountability—mentoring and knowledge sharing.</p>
        <ul class="text-sm mt-3 list-disc pl-5 text-gray-700">
          <li>Thought leadership</li><li>Mentoring networks</li><li>Public reporting</li>
        </ul>
      </article>
    </div>
  </section>

  <!-- PROCESS -->
  <section id="process" class="section">
    <h2 class="text-2xl font-semibold reveal">Our RAP process</h2>
    <div class="grid md:grid-cols-3 gap-6 mt-6">
      <div class="grid-card opacity-0 translate-y-4 reveal">
        <h3 class="font-semibold">1) Discover</h3>
        <p class="text-sm mt-2">Interviews, baseline and maturity check. Define goals aligned to business priorities.</p>
      </div>
      <div class="grid-card opacity-0 translate-y-4 reveal">
        <h3 class="font-semibold">2) Design</h3>
        <p class="text-sm mt-2">Co-design actions, owners, due dates and evidence types. Set governance cadence.</p>
      </div>
      <div class="grid-card opacity-0 translate-y-4 reveal">
        <h3 class="font-semibold">3) Deliver</h3>
        <p class="text-sm mt-2">Kick-off, change communications and dashboard enablement. Quarterly reporting.</p>
      </div>
    </div>
  </section>

  <!-- DELIVERABLES -->
  <section id="deliverables" class="section">
    <h2 class="text-2xl font-semibold reveal">What you get</h2>
    <ul class="list-disc ml-6 text-sm mt-4 max-w-3xl opacity-0 translate-y-4 reveal">
      <li>RAP document (Reflect/Innovate/Stretch/Elevate) co-designed and submission-ready</li>
      <li>RAP Dashboard provisioned to your M365 tenant (tracker, reporting, hub)</li>
      <li>Governance charter, roles & responsibilities and meeting templates</li>
      <li>Training plan mapped to actions with attendance & completion tracking</li>
      <li>Quarterly progress summary + board-ready slide/brief template</li>
    </ul>
  </section>

  <!-- TIMELINE -->
  <section id="timeline" class="section">
    <div class="p-6 border rounded-2xl opacity-0 translate-y-4 reveal bg-white">
      <h3 class="font-semibold">Typical timeline</h3>
      <ol class="list-decimal ml-5 text-sm mt-2">
        <li><strong>Weeks 1–3:</strong> Discovery & baseline, governance setup</li>
        <li><strong>Weeks 4–8:</strong> Co-design actions, dashboard configuration</li>
        <li><strong>Weeks 9–12:</strong> Kick-off, comms, training initiation</li>
        <li><strong>Quarterly:</strong> Progress reviews, evidence capture, reporting</li>
      </ol>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq" class="section">
    <h2 class="text-2xl font-semibold">RAP FAQs</h2>
    <div class="mt-6 grid gap-4">
      <details class="p-4 border rounded-xl reveal opacity-0 translate-y-4">
        <summary class="font-semibold">How long does a RAP take?</summary>
        <p class="mt-2 text-sm">Timelines vary by stage and size. Reflect can be planned in weeks; Innovate/Stretch often span 6–12 months with quarterly reporting.</p>
      </details>
      <details class="p-4 border rounded-xl reveal opacity-0 translate-y-4">
        <summary class="font-semibold">Do you help with evidence and reporting?</summary>
        <p class="mt-2 text-sm">Yes — the SharePoint RAP Dashboard manages actions, evidence and quarterly roll-ups with exports.</p>
      </details>
      <details class="p-4 border rounded-xl reveal opacity-0 translate-y-4">
        <summary class="font-semibold">How do we align RAP work with cultural training?</summary>
        <p class="mt-2 text-sm">We plan training streams alongside RAP actions and track completion against RAP goals.</p>
      </details>
    </div>
  </section>

  <!-- CTA -->
  <section class="section">
    <div class="p-6 border rounded-2xl flex flex-col md:flex-row items-center justify-between gap-4 opacity-0 translate-y-4 reveal">
      <div>
        <h3 class="font-semibold text-lg">Start with a discovery call</h3>
        <p class="text-sm text-gray-600">We’ll scope your current stage, priorities and timelines.</p>
      </div>
      <a href={`${base}contact/`} class="btn">Talk to us</a>
    </div>
  </section>

  <Fragment slot="footer"><Footer /></Fragment>

  <!-- Parallax (mobile fallback) + Reveal -->
  <script is:inline>
    const parallaxEls = document.querySelectorAll('[data-parallax]');
    const onScroll = () => {
      const y = window.scrollY || window.pageYOffset;
      parallaxEls.forEach(el => {
        if (window.matchMedia('(max-width: 767px)').matches) {
          const speed = parseFloat(el.dataset.speed || '0.15');
          el.style.transform = `translate3d(0,${y * speed * -0.4}px,0)`;
        }
      });
    };
    window.addEventListener('scroll', onScroll, { passive: true }); onScroll();

    const io = new IntersectionObserver((entries)=>{
      entries.forEach(e=>{
        if(e.isIntersecting){ e.target.classList.remove('opacity-0','translate-y-4'); io.unobserve(e.target); }
      });
    },{threshold:0.15});
    document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
  </script>

<section class="section">
    <div class="cta-banner">
      <div>
        <h3 class="text-xl font-semibold">Ready to move from intent to impact?</h3>
        <p class="text-white/90 text-sm">Book a discovery call and we’ll scope stage, priorities and timelines together.</p>
      </div>
      <a href={`${import.meta.env.BASE_URL}contact/`} class="btn bg-white text-[var(--brand-dark)] hover:bg-white/90">Talk to us</a>
    </div>
  </section>

</BaseLayout>
